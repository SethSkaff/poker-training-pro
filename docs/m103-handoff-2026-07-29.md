# M103 handoff — 2026-07-29

## Status

**M103 is open. Do not commit it or start M104.** The worktree is the active,
uncommitted Direction A implementation begun from commit `cf70102`. Preserve
all existing changes; do not reset or discard them.

The user approved Direction A and the later camera-distance reconciliation:
table scale/geometry, centred x position, `EYE_HEIGHT = 1.26 m`, fixed 52°
vertical FOV, ±32° look limits, safe inset, seat roots, and physical hero
foreground remain fixed. Only camera depth may be used for responsive framing.

Read before resuming:

- `C:\Users\19496\.codex\attachments\2efb3e51-c4c1-4c30-b45e-b84a263e40a9\goal-objective.md`
- `TODOS.md`
- `docs\desktop-3d-composition-decision.md`
- `docs\desktop-3d-architecture.md`
- `docs\desktop-3d-implementation-plan.md`

## What is currently implemented

- Direction A open five-opponent forward arc, capsule geometry, full-bleed
  procedural room, physical hero cards/chips/commitment, world-space pot
  plaques, physical dealer/blind markers, DOM accessibility parity, and
  forced-WebGL DOM fallback.
- The renderer now instances chip stacks. This dropped native draw calls from
  the rejected r17 value of 163 to roughly 62–70 without removing physical
  chips or raising the 150-call budget.
- `scripts/audit-packaged-3d-scene.mjs` uses real native Electron windows at
  1024×768, 1100×720, 1280×720, 1366×768, 1920×1080, and 2560×1080; it
  captures recenter/left/right frames, lifecycle/minimize, three real context
  recoveries, public-object parity, and a deterministic side-pot hand.
- The same harness was extended to write a true five-second silent native
  screenshot at 1366×768 and 1920×1080. Corresponding fixture coverage is in
  `scripts/audit-packaged-3d-scene.test.mjs`.
- Current (unverified) framing work in source:
  - at 1920, the depth solver requests a closer geometric rail envelope
    (`0.95`) to correct r19's undersized table;
  - this tapers back to `0.79` by 2560 so the ultrawide does not pull closer
    than the centred 16:9 gameplay zone;
  - room rear wall widened from 22 m to 36 m, architectural bays widened, and
    a physical elevated cove/horizon band was added to make ±32° room wings
    continuous and to put the designed horizon in the required screen band;
  - the physical turn ring is now suppressed for a hero turn. It rendered as
    an enormous foreground torus in r21 because the camera is the hero; hero
    turn state remains in the action band/live DOM, while opponent chair rings
    remain physical.

## Native evidence already valid (r19)

Package: `outputs\desktop-m103-open-arc-r19\win-unpacked\Poker Training Pro.exe`.

Passed reports, all from the packaged executable:

- `work\packaged-3d-scene-audit-webgl2-full-runner-showdown-3.json`
- `work\packaged-3d-scene-audit-webgl2-reduced-runner-showdown-3.json`
- `work\packaged-3d-scene-audit-webgl2-off-runner-showdown-3.json`
- `work\packaged-3d-scene-audit-forced-webgl-failure-reduced-runner-showdown-3.json`
- `work\packaged-3d-scene-audit-webgl2-reduced-scene-side-pot-0.json`
- `work\packaged-input-smoke.json`

Those reports establish normal/reduced/off WebGL, forced fallback, renderer
budget/performance, six native window sizes, public DOM/scene parity, mounted
fallback DOM, minimize/restore, real context recovery, deterministic main +
two-side-pot lanes, and broad genuine CDP pointer/keyboard/Gamepad API input.
The side-pot image is `work\packaged-3d-scene-webgl2-reduced-side-pots.png`.

## Independent review: r19 must not be accepted

The read-only independent verifier found these material M103 blockers:

1. 1920 recenter table was about 63% viewport width, below the required
   70–76% / 70–86% target and visually detached.
2. Visible horizon was around 35–43%, below required 18–28%.
3. ±32° panning exposed near-black empty wings instead of meaningful room.
4. Main pot plaque could visually overlap the centre player in the side-pot
   still.
5. r19 predated the new silent artifacts and did not satisfy the required
   silent-review evidence/approvals.
6. Terminal opponent action endpoints (fold/check/call/bet/raise/all-in) lack
   packaged still-frame evidence.
7. Input smoke does not yet prove scene-specific Q/E/X and controller camera
   inputs reach identical yaw/recenter states.
8. Forced fallback establishes mounted fallback and completion, but records
   only preflop public captures rather than a full public-runout parity matrix.

The r21/r22 source work directly addresses 1–3. Items 4–8 remain to be
addressed after the r22 visual check.

## Current r22 state

`outputs\desktop-m103-open-arc-r22\win-unpacked\Poker Training Pro.exe` was
created and source validation passed:

```powershell
npx vitest run src/scene3d/tableSceneModel.test.ts src/components/PokerTable.actingIndicator.test.ts src/components/PokerTable.camera.test.ts
# 38 tests passed
npm run build
git diff --check
```

The first r22 reduced-motion native audit was intentionally stopped at the
user's request before completion. Treat any aggregate report/screenshots with
current timestamps as incomplete; do not cite them as evidence. The exact
stopped command was:

```powershell
node scripts/audit-packaged-3d-scene.mjs --app 'outputs/desktop-m103-open-arc-r22/win-unpacked/Poker Training Pro.exe' --kind webgl2 --motion reduced --seed runner-showdown-3
```

Resume with that command. If it passes, inspect these first:

- `work\packaged-3d-scene-webgl2-reduced-1920x1080-silent-5s.png`
- `work\packaged-3d-scene-webgl2-reduced-1366x768-silent-5s.png`
- `work\packaged-3d-scene-webgl2-reduced-1024x768-recenter.png`

Check 1920 rail width, seated feel, hero-card prominence, the architectural
horizon band, and absence of the hero turn-ring. Then run full mode and inspect
all left/right frames for useful room wings before running off/fallback/side-pot
again against r22.

## Required remaining M103 work

1. Finish r22 native matrix and visual review. Fix root causes if any visual
   acceptance item fails; do not loosen FOV/table/seat rules.
2. Capture terminal opponent action stills for fold, check, call, bet, raise,
   and all-in. The likely extension point is
   `scripts/audit-packaged-3d-scene.mjs`; preserve the real runner and mounted
   controls rather than adding engine mocks.
3. Add/execute a packaged scene-camera input proof for pointer plus Q/E/X and
   controller camera commands, verifying equivalent left/recenter/right state
   without focusing the decorative canvas. Existing broad Gamepad proof is
   `scripts/audit-packaged-input-smoke.mjs`.
4. Strengthen forced-WebGL fallback evidence to compare all public runout beats
   with normal WebGL, not only the initial preflop capture.
5. Re-run an independent read-only M103 review only after fresh current-package
   evidence exists. Record both the five-second design review and independent
   review in `TODOS.md`.
6. Only after every exact M103 criterion passes: update `TODOS.md` with commands,
   reports, screenshots, measurements, and results; commit with task ID; then
   proceed to M104 automatically.

## Computer Use note

The Computer Use skill was initialized and its guidance/confirmation policy
read. It found an already-running **older** package at
`outputs\desktop-f06f-installed\Poker Training Pro.exe`, currently on a
checkpoint-resume prompt. The r22 launch did not create a second targetable
window because of Electron single-instance behavior. Do not use that older
window as r22 evidence or mutate its user checkpoint. Native CDP package audits
above do launch and inspect the r22 executable successfully.

## Worktree

No M103 commit exists. At stop, expected modified files include the earlier
M103 source/docs/audit set plus current edits to:

- `src\scene3d\tableSceneModel.ts`
- `src\scene3d\tableScene.ts`
- `scripts\audit-packaged-3d-scene.mjs`
- `scripts\audit-packaged-3d-scene.test.mjs`
- `src\components\PokerTable.accessibility.test.ts`

Run `git status --short` and `git diff --check` before continuing; preserve all
related dirty changes.
