# Checkpoint C3 receipt

- checkpoint: C3
- task IDs: T7, T8, T9
- implementation commit: `b4d30e6`
- source receipt basis: `git rev-parse HEAD` at implementation close
- external network/reviewer/model campaign: not used

## Required focused verification

All commands used the supported Node 22 runtime (`C:\Users\19496\.local\node22`) and exited 0.

```text
npx vitest run scripts/evaluation/referenceValues.test.ts scripts/evaluation/referenceCases.test.ts scripts/evaluation/wagerCandidates.test.ts scripts/evaluation/reviewPrototypeRunner.test.ts src/modes/handReviewPrototype.test.ts src/modes/handReview.test.ts src/modes/rational.test.ts src/modes/rationalEquityService.test.ts src/modes/wesleyIncident.test.ts src/engine/pots.test.ts src/engine/betting.test.ts
  11 files, 104 tests passed

npx tsc --noEmit --pretty false
  exit 0
```

The focused evidence covers the conditioning sweep at 33%, 75%, 150%, 500% and
1,300%; reconstructed fold/call/all-in-call closure and exact zero fold; joint
weighted worlds; side pots, ties and unequal stacks; unsupported continuation;
the 5x/10x negative controls; exact paired Monte Carlo draw IDs; observer-safe
support projection; flat/adverse/noisy/discontinuous review surfaces; the
one-chip and 25-chip contexts; the authoritative replay traversal; offline
wager floor/ceil expansion; legality/rack exceptions; and unchanged Rational,
equity-service, hand-review, engine and Wesley regressions.

## Preserved invariants

- Review v2 is an injected offline provider prototype and is not wired into the
  existing Game Review UI or default production selection.
- Wager expansion is offline; `quantizeWager` remains the sole live quantizer.
  Calls, all-ins, minimums, forced/state-derived amounts and exact legal
  boundaries retain engine targets.
- Reference support, numerical uncertainty, model confidence and grading
  eligibility remain separate. Missing external calibration remains pending.
- The source support seam is observer-scoped and has no strategy authority.
- Existing production candidates, RNG recipes, replay behavior, and Wesley
  protections were not tuned or replaced.

## Remaining evidence

Human/expert labels, empirical range calibration, approved tolerances, external
solver values, reviewer promotion and any live intervention/adoption remain
pending or outside this implementation scope.
