# C5 checkpoint receipt

Checkpoint C5 covers T13 observer histories and T14 offline adaptation experiments.

## Focused verification

Using Node 22.23.1:

```text
npx vitest run scripts/evaluation/observerHistory.test.ts scripts/evaluation/adaptationExperiment.test.ts --reporter=verbose
Test Files  2 passed (2)
Tests       7 passed (7)

npx tsc --noEmit --pretty false
passed
```

## Contract evidence

- Observer state is versioned and session-scoped. Canonical OpportunityEvent IDs are the deduplication key; duplicate replay is idempotent, late observers do not receive prior events, and a new session starts empty.
- Facing-pressure and unopened-preflop contexts use observable action categories. Per-player-hand counts, shown-card caveats, prior/config references, and a no-decay Dirichlet posterior are explicit. A zero-prior report is uninitialized rather than guessed.
- The legacy `PublicOpponentHistory` shape is only an explicitly labeled export; the corrected observer counters are not wired into live Normal policy.
- T14 uses declared finite scripted policies, fixed/adaptive paired schedules, deterministic predeclared seeds, public likelihood updates, and the T7 finite reference provider. Identical public likelihoods preserve prior odds.
- Payout/qualification calibration, human labels, external validation, and live adoption authority remain pending. No persistent live learning or sanity guard was added.
